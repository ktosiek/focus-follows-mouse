'use babel';

import FocusFollowsMouseView from '../lib/focus-follows-mouse-view';

describe('FocusFollowsMouseView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
